server:
  port: 8082

spring:
  application:
    name: digital-garage-service
  couchbase:
    connection-string: couchbase://localhost
    user-name: admin
    password: password
  data:
    couchbase:
      bucket-name: digital_garage

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/MotoNexus
